self.addEventListener('fetch', () => {
    // This empty fetch handler is required to make the app installable as a PWA.
    // It doesn't cache anything, allowing the browser to handle requests normally.
});
