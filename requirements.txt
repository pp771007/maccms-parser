# Web 框架
Flask>=2.0.0

# HTTP 客戶端庫及其依賴
requests>=2.25.1
urllib3>=1.26.5  # requests 的核心依賴，明確指定以確保兼容性

# WSGI 伺服器
gunicorn>=20.1.0

# 協程庫 (解決 RecursionError 的關鍵)
gevent>=21.8.0
greenlet>=1.1.0  # gevent 的核心依賴，必須一起更新

# 高性能 JSON 庫
ujson>=5.0.0